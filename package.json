{
  "name": "jev-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live tests for TypeSafe Jev (System One) via OpenRouter's Decisions API",
  "scripts": {
    "start": "tsx src/run.ts",
    "cases": "tsx src/run.ts --list",
    "test": "tsx src/run.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "tsx": "^4.23.12",
    "typescript": "^5.9.2"
  }
}
